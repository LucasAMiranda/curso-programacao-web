/**
 * Autor: Lucas 
 * Data: 22/08/2023
 * Assunto: percorrendo os elemento dentro do array com método forEach
 */

const Frutas = ['Mamão', 'banana', 'Melancia', 'Abacaxi'];

Frutas.forEach(function(fruta){
    console.log(fruta); // O que estiver aqui será executado para cada item no array, ou seja, o primeiro
})